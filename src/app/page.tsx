import Home from '@/app/_components/Home'

/**
 * Home Page
 * 
 * Clean implementation that only handles the home route.
 * File-system routing handles all other routes automatically.
 */
export default function HomePage() {
  return <Home />
}
